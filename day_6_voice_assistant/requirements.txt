# Core ML / NLP
scikit-learn>=1.3.0
numpy>=1.24.0
joblib>=1.3.0

# Voice input (optional — falls back to text input if unavailable)
SpeechRecognition>=3.10.0
PyAudio>=0.2.13

# Text-to-speech (optional — falls back to print if unavailable)
pyttsx3>=2.90

# Enhanced NLP (optional)
spacy>=3.7.0
nltk>=3.8.0

# Data handling (used by other day projects, kept for consistency)
pandas>=2.0.0

# Optional: GPU-accelerated speech recognition
# openai-whisper>=20231117
